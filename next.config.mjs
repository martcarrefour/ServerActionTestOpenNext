/** @type {import('next').NextConfig} */

if (!process.env.NODE_ENV) {
	process.env.NODE_ENV = 'production'; // Или 'development', в зависимости от ветки
  }
const nextConfig = {
	
};

export default nextConfig;
